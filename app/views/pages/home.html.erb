  <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://wallpapercave.com/wp/ZaHQ4Jf.jpg);">    <div class="container">
      <h1>Moveas vous <strong>fait voyager</strong> comme <strong>vos héros préférés </strong>!</h1>
      <p>N'avez-vous jamais rêvé de parcourir le monde à la vitesse de la lumière ?</p>
       <% if user_signed_in? %>
          <%= link_to 'Publier sur Movease', new_vehicule_path, class: "btn btn-flat" %>
          <% else %>
        <%= link_to 'Se connecter', user_session_path, class: "btn btn-flat" %>
          <% end %>
    </div>
  </div>

  <div class="container-filter">
<div class="filters flex">
  <div class="textfilter">Filtres</div>
  <div class="filterlist flex">
    <div class="filter flex">
        <div class="iconfilter flex">
              <i class="fa-solid fa-jet-fighter"></i>
        </div>
                <%= link_to "Aérien", root_path(query: 'Aérien') %>

    </div>
    <div class="filter flex">
        <div class="iconfilter flex">
              <i class="fa-solid fa-car-side"></i>
        </div>
                <%= link_to "terrestre", root_path(query: 'terrestre') %>

    </div>
    <div class="filter flex">
        <div class="iconfilter flex">
              <i class="fa-solid fa-anchor"></i>
        </div>
               <%= link_to "maritime", root_path(query: 'maritime') %>

    </div>
    <div class="filter flex">
        <div class="iconfilter flex">
            <i class="fa-regular fa-hand-spock"></i>
        </div>
                <%= link_to "multi-tache", root_path(query: 'multi-tache') %>

    </div>

  </div class="search-bar">
    <%= form_with url: root_path, method: :get, class: "d-flex search-bar" do %>
      <%= text_field_tag :query,
            params[:query],
            class: "form-control",
            placeholder: "Find by name or category"
        %>
      <%= submit_tag "Search", name: "", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>

</div>
<div class="custom-container">

  <div class="map"
    data-controller="map"
    data-map-markers-value="<%= @markers.to_json %>"
    data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>

  <%= render 'search' %>
</div>
