<h1>Congrats ! You have booked :</h1>
<div class="card-holder">
  <div class="card">
    <img src= >
    <h5><%= @reservation.vehicule.name%></h5>
    <p><%=@vehicule.description%></p>
    <ul>
      <li>booked for :<%= @reservation.date%></li>
    </ul>
    <div class="links">
      <%= link_to 'Back to dashboard', dashboard_path %>
      <%= link_to 'Back to announcements', root_path %>
      <%= link_to 'Contact the owner', user_messages_path(@vehicule.user) %>
    </div>
  </div>
</div>
<h1>Thank your for your reservation</h1>

<div class="container">
  <div class="row">
    <div class="col">
      <h1>Thank you for the reservation</h1>
        <div>
          <%= @reservation.vehicule.name %>
        </div>
        <%= @reservation.date %>
    </div>
  </div>
</div>
